
        -- Find records in agg_data with non-positive installs (installs <= 0)
        SELECT COUNT(*) as cnt
        FROM `data-quality-project-1.dq_data_set.agg_data`
        WHERE installs <= 0
    